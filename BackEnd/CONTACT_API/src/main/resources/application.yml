spring:
  application:
    name: 15_CONTACT_API
  
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/HIS
    driver-class-name: com.mysql.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
     
    show-sql: true
    
server:
  port: 9090
